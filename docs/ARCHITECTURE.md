# Architecture

Publieke API's worden via Python-clients opgehaald, gevalideerd, uitgelijnd en omgezet naar dagelijkse features. Daarna volgen scoreberekening, historische analogieën, walk-forward backtest, bewijskwaliteit, evidence ledger en statische JSON voor de website.

